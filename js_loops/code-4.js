let word = "langue" ;
for (let i = 0 ; i < word.length ; i++) {
        console.log(i) ;
        console.log(word[i]) ;
        // boucle dans un tableau avec un indice debut [0] et fin du tablo [length]
}