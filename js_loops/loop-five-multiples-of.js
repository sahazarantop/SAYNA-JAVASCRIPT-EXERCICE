function cinqMultiplesOf(params) {
        let multiply
        for (let i = 0; i < 5; i++) {
                multiply = i * params
                console.log(multiply);
        }
}

cinqMultiplesOf(7) ;
cinqMultiplesOf(3) ;